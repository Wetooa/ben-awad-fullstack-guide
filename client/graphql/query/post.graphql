query Posts($cursor: String!, $limit: Int!) {
  posts(cursor: $cursor, limit: $limit) {
    posts {
      id
      title
      text
      createdAt
      updatedAt
      textSnippet
    }
    hasMore
  }
}
