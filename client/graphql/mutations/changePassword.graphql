mutation ChangePassword(
  $reTypePassword: String!
  $newPassword: String!
  $token: String!
) {
  changePassword(
    reTypePassword: $reTypePassword
    newPassword: $newPassword
    token: $token
  ) {
    ...RegularUserResponse
  }
}
